package org.kakara.engine.ui.constraints;

/**
 * The side of the component that the constraint is to compare to.
 * @since 1.0-Pre1
 */
public enum ComponentSide {
    TOP,
    BOTTOM,
    LEFT,
    RIGHT
}
